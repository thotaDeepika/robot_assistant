name: Greeting Workflow
on: [push]

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Copy Repo to Runner
        uses: actions/checkout@v4

      - name: Install pytest
        uses : python3 -m pip install pytest

      - name: Run a greeting
      
        run: echo "The CI pipeline has successfully started!"

      - name: Run Python Tests
        run : pyton3 -m pytest
        
